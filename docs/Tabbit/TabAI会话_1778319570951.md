好，我直接拉。

所有 API 都拿到了。下面就是完整的可落地方案，三份产物直接给你。

---

## **一、关键 API 摘要（已核实）**

| 模块 | 签名 | 备注 |
|---|---|---|
| `CDPClient` 构造 | `new CDPClient({ host?, port?, targetFilter? })` | 默认 `localhost:9222`，target 默认匹配 Trae/SOLO 标题 |
| 连接 | `await client.connect()` | 自动 enable Runtime/DOM/Input/Page，并启动 10s 心跳 |
| 断开 | `client.disconnect()` | |
| `scanTasks` | `(cdp) => Promise<TaskInfo[]>` | TaskInfo 含 `index, name, status, isSelected` |
| `findTaskSlot` | `(tasks, taskName) => number` | 返回 -1 表示未找到 |
| `switchTask` | `(cdp, slot: number) => Promise<void>` | **接受的是 index 不是 name**，找不到抛 `TaskSwitchError` |
| `fillPrompt` | `(cdp, prompt: string) => Promise<void>` | 用 `Input.insertText` 走 Lexical 原生事件 |
| `submit` | `(cdp) => Promise<void>` | 用 `Input.dispatchKeyEvent(Enter)` |
| `waitResponse` | `(cdp, opts?) => Promise<string>` | opts 含 `taskName, timeoutMs, pollMs, recoveryPolicy, logger`，返回 AI 文本 |

`switchTask` 这个签名是关键发现——它要的是 index 不是 name。所以注入流程必须先 `scanTasks` 拿到列表，再用 `findTaskSlot` 把任务名翻译成 index，再 `switchTask(cdp, idx)`。这一点之前的猜测对了一半。

---

## **二、产物 1：`mvp-runner/scripts/inject-prompt.ts`**

直接在仓库里新建这个文件，内容如下：

```typescript
// scripts/inject-prompt.ts
// 一次性 CLI：从终端注入 prompt 到指定 Trae 任务，可选等待回复
// 用法见文件末尾 USAGE 字符串

import createDebug from 'debug';
import { CDPClient } from '../src/cdp/client.js';
import { scanTasks, findTaskSlot } from '../src/actions/scan-tasks.js';
import { switchTask } from '../src/actions/switch-task.js';
import { fillPrompt } from '../src/actions/fill-prompt.js';
import { submit } from '../src/actions/submit.js';
import { waitResponse } from '../src/actions/wait-response.js';

const debug = createDebug('mvp:inject');

interface CliArgs {
  task: string;
  prompt: string;
  wait: boolean;
  timeoutMs: number;
  traceId?: string;
  cdpHost: string;
  cdpPort: number;
}

const USAGE = `
Usage:
  npx tsx scripts/inject-prompt.ts --task <NAME> --prompt <TEXT> [options]

Required:
  --task <NAME>          Task name shown in Trae sidebar, e.g. PMCLI / DEVCLI / TESTCLI
  --prompt <TEXT>        Prompt text. Use quotes if it contains spaces.

Options:
  --wait                 Wait for AI response and print it to stdout
  --timeout <ms>         Wait timeout in ms, default 300000 (5 min)
  --trace-id <id>        Marker echoed in logs, useful for regression test correlation
  --cdp-host <host>      Default localhost
  --cdp-port <port>      Default 9222
  -h, --help             Show this help

Exit codes:
  0   Success (submitted; if --wait, also received reply)
  2   Task name not found in sidebar
  3   Fill / submit failed
  4   Wait timeout
  5   CDP connection failed
  64  Bad arguments
`;

function parseArgs(argv: string[]): CliArgs {
  const args: Partial<CliArgs> = {
    wait: false,
    timeoutMs: 300000,
    cdpHost: 'localhost',
    cdpPort: 9222,
  };
  for (let i = 0; i < argv.length; i++) {
    const a = argv[i];
    const next = () => argv[++i];
    switch (a) {
      case '--task': args.task = next(); break;
      case '--prompt': args.prompt = next(); break;
      case '--wait': args.wait = true; break;
      case '--timeout': args.timeoutMs = Number(next()); break;
      case '--trace-id': args.traceId = next(); break;
      case '--cdp-host': args.cdpHost = next(); break;
      case '--cdp-port': args.cdpPort = Number(next()); break;
      case '-h':
      case '--help':
        console.log(USAGE);
        process.exit(0);
      default:
        console.error(`[inject] unknown arg: ${a}`);
        console.error(USAGE);
        process.exit(64);
    }
  }
  if (!args.task || !args.prompt) {
    console.error('[inject] --task and --prompt are required');
    console.error(USAGE);
    process.exit(64);
  }
  return args as CliArgs;
}

async function main() {
  const args = parseArgs(process.argv.slice(2));
  const tag = args.traceId ? `[inject:${args.traceId}]` : '[inject]';
  const startedAt = Date.now();

  console.log(`${tag} task=${args.task} promptLen=${args.prompt.length} wait=${args.wait}`);

  const cdp = new CDPClient({ host: args.cdpHost, port: args.cdpPort });

  try {
    try {
      await cdp.connect();
    } catch (e) {
      console.error(`${tag} CDP connect failed: ${(e as Error).message}`);
      process.exit(5);
    }
    console.log(`${tag} CDP connected`);

    // 1. 扫描任务，把名字翻译成 index
    const tasks = await scanTasks(cdp);
    console.log(`${tag} sidebar tasks: ${tasks.map(t => `${t.index}:${t.name}(${t.status})`).join(', ')}`);
    const slot = findTaskSlot(tasks, args.task);
    if (slot < 0) {
      console.error(`${tag} task "${args.task}" not found`);
      process.exit(2);
    }

    // 2. 切换任务
    try {
      await switchTask(cdp, slot);
    } catch (e) {
      console.error(`${tag} switchTask failed: ${(e as Error).message}`);
      process.exit(3);
    }
    console.log(`${tag} switched to slot ${slot}`);

    // 3. 填入 prompt
    try {
      await fillPrompt(cdp, args.prompt);
    } catch (e) {
      console.error(`${tag} fillPrompt failed: ${(e as Error).message}`);
      process.exit(3);
    }
    console.log(`${tag} prompt filled`);

    // 4. 提交
    try {
      await submit(cdp);
    } catch (e) {
      console.error(`${tag} submit failed: ${(e as Error).message}`);
      process.exit(3);
    }
    const submitMs = Date.now() - startedAt;
    console.log(`${tag} submitted (${submitMs}ms elapsed)`);

    // 5. 可选：等待回复
    if (args.wait) {
      console.log(`${tag} waiting for reply (timeout=${args.timeoutMs}ms)...`);
      try {
        const reply = await waitResponse(cdp, {
          taskName: args.task,
          timeoutMs: args.timeoutMs,
        });
        const totalMs = Date.now() - startedAt;
        console.log(`${tag} reply received (${reply.length} chars, total ${totalMs}ms)`);
        console.log('---REPLY-BEGIN---');
        console.log(reply);
        console.log('---REPLY-END---');
      } catch (e) {
        const msg = (e as Error).message;
        console.error(`${tag} waitResponse failed: ${msg}`);
        process.exit(msg.includes('超时') || msg.includes('timeout') ? 4 : 3);
      }
    }
  } finally {
    cdp.disconnect();
  }
}

main().catch((e) => {
  console.error('[inject] fatal:', e);
  process.exit(1);
});
```

把这个文件加到仓库后，在 `mvp-runner/` 目录下执行：

```bash
# 冒烟：发到 PMCLI 但不等回复
npx tsx scripts/inject-prompt.ts --task PMCLI --prompt "echo hello from cdp"

# 完整：发到 PMCLI 并等回复打印到 stdout
npx tsx scripts/inject-prompt.ts --task PMCLI --prompt "写一个 hello world" \
  --wait --timeout 120000 --trace-id smoke-001
```

---

## **三、产物 2：`mvp-runner/scripts/regression-step2.sh`**

Step 2 的双任务并发触发器，专门复现任务隔离 bug。注意我用 DEVCLI 作为干扰任务，因为侧边栏里 DEVCLI 是另一个 task-item，刚好测试跨任务作用域：

```bash
#!/usr/bin/env bash
# Step 2 双任务并发：在 PMCLI 思考期间塞入 DEVCLI 干扰任务
# 验证修复后：飞书收到的 PMCLI 回复必须是 PRD 摘要，而不是"现在几点"

set -e
cd "$(dirname "$0")/.."

OUT=comms/reports/regression-2026-05-09/step2
mkdir -p "$OUT"

PMCLI_PROMPT="写一份 500 字的 PRD 摘要，主题是任务隔离漏洞修复，需要包含背景、核心方案、验收标准三段，每段不少于 150 字"
DEVCLI_PROMPT="现在几点"

echo "$PMCLI_PROMPT" > "$OUT/pmcli-prompt.txt"
echo "$DEVCLI_PROMPT" > "$OUT/devcli-prompt.txt"

echo "[step2] launching PMCLI long task..."
npx tsx scripts/inject-prompt.ts \
  --task PMCLI \
  --prompt "$PMCLI_PROMPT" \
  --wait --timeout 240000 \
  --trace-id step2-pmcli \
  > "$OUT/pmcli-reply.txt" 2> "$OUT/pmcli-stderr.txt" &
PMCLI_PID=$!

# 给 PMCLI 5 秒进入 thinking 状态
sleep 5

echo "[step2] injecting DEVCLI distractor..."
npx tsx scripts/inject-prompt.ts \
  --task DEVCLI \
  --prompt "$DEVCLI_PROMPT" \
  --wait --timeout 30000 \
  --trace-id step2-distractor \
  > "$OUT/devcli-reply.txt" 2> "$OUT/devcli-stderr.txt" || true

echo "[step2] waiting PMCLI to finish..."
wait $PMCLI_PID || true

echo "[step2] outputs in $OUT"
```

跑完后 `pmcli-reply.txt` 里 `---REPLY-BEGIN---` 与 `---REPLY-END---` 之间的内容如果是 PRD 摘要，Step 2 PASS；如果出现"现在几点"或时间字样，说明跨任务数据泄漏 bug 仍在，FAIL。

---

## **四、产物 3：发给 Trae 的最终提示词（替换之前的版本）**

````markdown
# 任务：实施 inject-prompt CLI 并跑完任务隔离 P0 回归测试

## 强制约束
1. 每完成一个 Step 必须立即 commit + push 到 trae/solo-agent-7GnOo7
2. commit message 前缀：`test(regression):` 或 `chore(scripts):`
3. 任何 Step 失败立即停止，输出现场快照（runner 日志末 100 行 + recovery-audit.jsonl 末 50 行）

## Step 0：搭建 inject-prompt 工具
1. 在 mvp-runner/scripts/ 新建 inject-prompt.ts，内容由用户提供（见上文产物 1，照抄）
2. 在 mvp-runner/scripts/ 新建 regression-step2.sh，内容照抄产物 2，chmod +x
3. 验证 TypeScript 编译：`npx tsc --noEmit`
4. 冒烟：
   ```bash
   cd mvp-runner
   npx tsx scripts/inject-prompt.ts --task PMCLI --prompt "echo smoke test" --trace-id smoke-001
   ```
   预期 stdout 含 `submitted`，Trae IDE 中 PMCLI 输入框被填入并提交。
   如果 exit code != 0，停止，把 stderr 贴出来。
5. commit + push：`chore(scripts): add inject-prompt CLI and step2 driver`

产物：`comms/reports/regression-2026-05-09/00-preflight.md` 记录 tsc 与冒烟输出。

## Step 1：单任务基线
```bash
mkdir -p comms/reports/regression-2026-05-09/step1
cd mvp-runner
npx tsx scripts/inject-prompt.ts --task PMCLI \
  --prompt "写一个 hello world 的 python 函数" \
  --wait --timeout 120000 --trace-id step1 \
  | tee ../comms/reports/regression-2026-05-09/step1/run.log
```
verdict：run.log 中 `---REPLY-BEGIN---` 后的内容包含 python 代码片段 → PASS。
飞书侧也应收到对应卡片消息（mvp-runner 主进程要保持运行）。
截图飞书消息保存到 step1/lark-message.png。
commit：`test(regression): step 1 baseline <PASS|FAIL>`

## Step 2：双任务并发（核心 bug 还原）
```bash
cd mvp-runner
bash scripts/regression-step2.sh
```
verdict：
- `step2/pmcli-reply.txt` 的 REPLY 段必须是 PRD 摘要（含"任务隔离""验收标准"等关键词）
- 不能出现"现在几点"或时间字样
- 截图飞书 PMCLI 卡片消息保存到 step2/lark-message.png
- 写 step2/verdict.md 含 PASS/FAIL 与 REPLY 前 200 字

如果 FAIL，**停止后续 Step**，把整个 step2/ 目录 commit + push 后等待人工指示。
PASS 则 commit：`test(regression): step 2 concurrent PASS`

## Step 3：recovery 路径
1. 启动 PMCLI 长任务：
   ```bash
   npx tsx scripts/inject-prompt.ts --task PMCLI \
     --prompt "写一份 1000 字的技术文档..." \
     --wait --timeout 300000 --trace-id step3 &
   ```
2. 等 5 秒后人工在 Trae IDE 中点击"停止"按钮制造 frozen
3. 等待 mvp-runner 心跳层触发 recovery（观察 logs/recovery-audit.jsonl 出现 click-retry 条目）
4. 等 PMCLI 自动恢复完成，检查回复
5. 物证：step3/runner.log（grep "DOM stability after recovery"）+ recovery-audit.jsonl 副本 + lark-message.png + verdict.md

commit：`test(regression): step 3 recovery <PASS|FAIL>`

## Step 4：汇总报告
写 `comms/reports/regression-2026-05-09/REGRESSION_REPORT.md`，模板见之前提示词。
基于 trae/solo-agent-7GnOo7 创建 PR 到 main，标题：
`test: task-isolation P0 regression results - <ACCEPTED|REJECTED>`

## 报告频率
每完成一个 Step 在本对话回复一次：`✅ Step N done, commit <sha>, pushed`
````

---

## **五、给你的执行顺序**

第一步把产物 1 的代码复制到本地仓库 `mvp-runner/scripts/inject-prompt.ts`，产物 2 复制到 `mvp-runner/scripts/regression-step2.sh`，本机 `chmod +x` 并跑一次冒烟（产物 1 文档里第一条命令）。这一步如果 CDP 连不上 Trae，会直接报 exit 5，把 stderr 贴回来我帮你定位是 9222 端口没开还是 targetFilter 匹配不到。冒烟通过后，再把产物 3 的提示词整段发给 Trae SOLO 让它接手跑回归。

如果你不想分两步、想让 Trae 一气呵成把脚本也写了，把产物 1 和产物 2 的代码块整段贴到产物 3 的 Step 0 里替换那两行"内容由用户提供，照抄"即可，Trae 会自己创建文件。但我建议**脚本你自己创建**——这套脚本是验证工具，不该让被验证方（DEVCLI）自己写，避免它在脚本里埋彩蛋来美化测试结果。

*内容由 AI 生成仅供参考*