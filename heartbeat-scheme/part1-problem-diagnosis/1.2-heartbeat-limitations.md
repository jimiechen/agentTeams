# 1.2 为什么固定频率心跳不够用

假设用 5 秒固定频率采集所有信号：
- 检测 model-stalled 需要 30s 窗口，5 秒粒度浪费 6 倍采样
- 检测 terminal-hang 需要立即响应，15 秒粒度则慢 3 倍
- 深度指标（token 估算、按钮快照）单次采集 200ms+，5 秒频率累计开销占 CPU 4%

**根本矛盾**：不同模式的**探测成本**（DOM 扫描深度）和**时效性要求**（多久必须发现）不匹配。固定频率要么浪费资源，要么错过时效窗口。
